{
  "buildCommand": "cd packages/shared && npx tsc && cd ../server && npx prisma generate && cd ../web && npm install && npm run build",
  "outputDirectory": "packages/web/dist",
  "installCommand": "npm install --workspaces",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/eval/:path*", "destination": "/api" }
  ]
}
